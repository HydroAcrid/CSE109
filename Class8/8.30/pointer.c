#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

int main(int argc, char **argv) {

  // char *path = getenv("PATH");
  // /*

  // 1. Get PATH into the program
  // /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

  // 2. Split out the first directory from the PATH
  // /usr/local/sbin

  // 3. Maybe add a / to the end of that directory
  // /usr/local/sbin/

  // 4. Add filename to thend of that directory
  // /usr/local/sbin/gcc

  // 5. Check to see if file exists and is executable
  // int found = access(pathname,X_OK);

  //   */

  // char my_arg[100];
  // strcpy(my_arg,argv[1]);
  
  // printf("%s\n", argv[1]);
  // printf("%s\n", my_arg);

  // argv[1][1] = 'W';

  // printf("%s\n", argv[1]);
  // printf("%s\n", my_arg);

  char* foo = *foo + 1;
  //int* test1 = *foo + 1;
  printf("%s\n", foo);


  return 0;
}