//Prints out directories that are not hidden 
void show_directory(char* directory);

//Prints out all directories, regardless of whether they're hidden or not 
void show_directory_all(char* directory);

//Prints out dirrectories that are not hidden and provides more information on each directory 
void show_directory_long(char* directory);

//Prints all directories and provides more information on each directory 
void show_directory_long_all(char* directory);